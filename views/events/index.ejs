<%layout('./layouts/boilerplate')%>

<% for (let event of events){ %>
   
    <div class="card mb-3"  >
        <div class="row">
         
         <div class="col-md-8">
           <div class="card-body">
             <h5 class="card-title">
               <%= event.title %>
               
             </h5>
             <p class="card-text"><%= event.description %></p>
             <p class="text-muted"><%= event.location %></p>
             <a href="/events/<%= event._id %>" class="btn  btn-primary">View <%= event.title %></a>
           </div>
         </div>
        </div>
       </div>

    <%}%>

  